services:
  - type: web            # ← было: worker
    name: rubicon-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python rubicon_bot.py
